// import { ethers } from 'hardhat'

async function main() {
  //   const VotingFactory = await ethers.getContractFactory('Voting')
  //   const voting = await VotingFactory.deploy()
  //   const votingContract = await voting.deployed()
  //   console.log('Voting contract deployed:', votingContract)
  //   console.log('Voting deployed to address:', votingContract.address)
}

main().catch((error) => {
  console.error(error)
  process.exitCode = 1
})
